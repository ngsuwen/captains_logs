<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>New Log</title>
</head>

<body>
    <h1>Edit Page</h1>
    <form action='/api/<%=data._id%>/?_method=PUT' method="POST">
        <label for="title">Title</label>
        <input type="text" name="title" id="title" value=<%=data.title%>>
        <label for="entry">Entry</label>
        <input type="text" name="entry" id="entry" value=<%=data.entry%>>
        <label for="shipIsBroken">Ship is Broken</label>
        <input type="checkbox" name="shipIsBroken" id="shipIsBroken"
            <% if(data.shipIsBroken === true){ %> 
                checked 
            <% } %>
        />
        <input type="submit" value="Edit Log" />
    </form>
</body>

</html>