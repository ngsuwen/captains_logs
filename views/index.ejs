<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Logs</title>
</head>
<body>
    <h1>Captain's Logs</h1>
    <form action='/new'>
      <button>New</button>
   </form>
    <ul>
        <% for (let i = 0; i < logs.length; i++) { %>
        <li>
          <a href="/<%=logs[i]._id%>">
            <%= logs[i].title %>
          </a>
          <form action='/api/<%=logs[i]._id%>/?_method=DELETE' method="POST">
            <input type="submit" value="DELETE"/>
         </form>
         <a href="/<%=logs[i]._id%>/edit">
          Edit
         </a>
        </li>
        <% } %>
      </ul>
</body>
</html>